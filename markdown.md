```python
print('hello every')
print(" i am 23 years old")
def nember(x):
	if x < 10:
		print("你输入的数字一定小于10对吧")
	else:
    	print("行，你比我大，你牛逼")
```
你一定知道我是谁吧，没错，我就是**WYB**。
看到这里上一行的 *WYB* 应该加粗了吧，这一行应该是斜体吧
# 这应该是一级标题
## 接下来就是二级标题
### 那这个一定是三级标题
#### 这就是四级了吧应该
##补充小技巧
1.  写的时候随时点「Preview」标签，看实时渲染效果，不用等提交；
2.  文件名最好改成 `README.md`，GitHub会自动把它渲染成仓库的主页，别人打开就能看到；
3.  所有标记（比如 `#`、`-`）后面都要加一个空格，不然可能不生效。
5.  |时间|地点|任务|
    |:--:|:--:|:--:|
     |2001|china|study|
    |2002|us|sleep|
